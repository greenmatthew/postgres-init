# Required PostgreSQL admin connection parameters
PG_HOST=postgres                   # Hostname of your PostgreSQL server
PG_ADMIN_USER=postgres             # PostgreSQL admin username
PG_ADMIN_PASSWORD=password         # PostgreSQL admin password

# Required application database parameters
APP_DB_NAME=app                  # Name of the database to create
APP_DB_USER=app                  # User to create and grant access
APP_DB_PASSWORD=password         # Password for the user

# Optional parameters
# PG_PORT=5432                       # PostgreSQL port (default: 5432)
# WAIT_INTERVAL=10                   # Seconds to wait between connection attempts (default: 10)
# DB_EXTENSIONS=pg_trgm,vectors      # Comma-separated list of extensions to enable (optional)